import { ImageShadowDirective } from './image-shadow.directive';

describe('ImageShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageShadowDirective();
    expect(directive).toBeTruthy();
  });
});

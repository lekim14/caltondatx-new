import { VScrollToDirective } from './v-scroll-to.directive';

describe('VScrollToDirective', () => {
  it('should create an instance', () => {
    const directive = new VScrollToDirective();
    expect(directive).toBeTruthy();
  });
});

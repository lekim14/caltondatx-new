import { VAnimateOnViewDirective } from './v-animate-on-view.directive';

describe('VAnimateOnViewDirective', () => {
  it('should create an instance', () => {
    const directive = new VAnimateOnViewDirective();
    expect(directive).toBeTruthy();
  });
});
